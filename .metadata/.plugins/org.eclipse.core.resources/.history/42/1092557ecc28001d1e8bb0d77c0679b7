package classe;

public class Data {
	int dia = 0;
	int mes;
	int ano;
	
	String obterDataFormatada() {
		String dia = (dia < 10) ? "0"+Integer.toString(dia) : Integer.toString(dia);
		return  + "/" + Integer.toString(mes) + "/" + Integer.toString(ano);
	}
}
